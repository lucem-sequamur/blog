import { SrcRebasePipe } from './src-rebase.pipe';

describe('SrcRebasePipe', () => {
  it('create an instance', () => {
    const pipe = new SrcRebasePipe();
    expect(pipe).toBeTruthy();
  });
});
