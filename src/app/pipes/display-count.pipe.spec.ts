import { DisplayCountPipe } from './display-count.pipe';

describe('DisplayCountPipe', () => {
  it('create an instance', () => {
    const pipe = new DisplayCountPipe();
    expect(pipe).toBeTruthy();
  });
});
